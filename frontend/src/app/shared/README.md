# Shared Module

Responsibilities
- Reusable UI components and helpers shared across pages.

Inputs/Outputs
- Inputs: component inputs, content projection, UI events.
- Outputs: reusable UI building blocks and emitted events.

Invariants
- Shared components stay UI-focused and reusable.
