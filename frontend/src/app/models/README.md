# Models Module

Responsibilities
- Shared TypeScript interfaces and DTO shapes used across the UI.

Inputs/Outputs
- Inputs: used by services and components for typing.
- Outputs: compile-time type safety only.

Invariants
- Model shapes should match backend API responses.
